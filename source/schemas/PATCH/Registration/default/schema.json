{
  "type": "object",
  "properties": {
    "people_count": {
      "type": "integer"
    },
    "channel": {
      "type": "string"
    },
    "confirmed_channel": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "vkontakte": {
      "type": "string"
    },
    "telegramBot": {
      "type": "string"
    },
    "is_canceled": {
      "type": "boolean"
    },
    "is_confirm": {
      "type": "boolean"
    },
    "status": {
      "type": "string",
      "enum": [
        "Created",
        "Confirmed",
        "Cancelled"
      ]
    },
    "lineup": {
      "type": "string",
      "enum": [
        "Main",
        "Reserve"
      ]
    }
  },
  "required": [],
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "channel": {
            "const": "email"
          }
        },
        "required": [
          "channel"
        ]
      },
      "then": {
        "required": [
          "channel",
          "confirmed_channel"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "channel": {
            "const": "telegramBot"
          }
        },
        "required": [
          "channel"
        ]
      },
      "then": {
        "required": [
          "channel",
          "confirmed_channel",
          "telegramBot"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "channel": {
            "const": "vkontakte"
          }
        },
        "required": [
          "channel"
        ]
      },
      "then": {
        "required": [
          "channel",
          "confirmed_channel",
          "vkontakte"
        ]
      }
    }
  ]
}