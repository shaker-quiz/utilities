import { Interval } from './interval.js'

export const CategoryRegistrationIntervals = Object.freeze({
  [Interval['1 day']]: Object.freeze([
    Interval['1 day'],
  ]),

  [Interval['3 days']]: Object.freeze([
    Interval['3 days'],
  ]),

  [Interval['1 week']]: Object.freeze([
    Interval['1 week'],
  ]),

  [Interval['1 month']]: Object.freeze([
    Interval['1 month'],
  ]),

  [Interval['3 months']]: Object.freeze([
    Interval['3 months'],
  ]),

  [Interval['6 months']]: Object.freeze([
    Interval['6 months'],
  ]),

  [Interval['1 year']]: Object.freeze([
    Interval['1 year'],
  ]),

  [Interval['all time']]: Object.freeze([]),
})
